{
    "name": "fx-tests",
    "version": "0.0.0",
    "private": true,
    "description": "Command-line access to the Firefox test-health data behind https://tests.firefox.dev/",
    "license": "MPL-2.0",
    "type": "module",
    "bin": {
        "fx-tests": "./bin/fx-tests.js"
    },
    "engines": {
        "node": ">=20"
    },
    "scripts": {
        "build": "echo 'No CLI to build yet: cli/bin/fx-tests.ts arrives in step 4 of docs/PLAN.md. Use npm run typecheck and npm test.' >&2 && exit 1",
        "typecheck": "tsc --noEmit",
        "fixtures": "node --experimental-strip-types tools/make-fixtures.ts",
        "test": "node --experimental-strip-types --test 'test/**/*.test.ts'"
    },
    "devDependencies": {
        "@types/node": "^22.20.1",
        "esbuild": "^0.25.0",
        "typescript": "^5.7.0"
    }
}
